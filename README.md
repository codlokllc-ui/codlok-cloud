# Codlok Cloud

Codlok Cloud is a reusable, provider-neutral backend control plane for products.

## Local setup

1. Copy `.env.example` to `.env`.
2. Add development credentials locally.
3. Run `npm install`.
4. Run `npm run dev`.

Never commit provider credentials or production secrets.
